export class Card {

    constructor(id, visitor, bookName, borrowDate, returnDate) {
        this._id = id
        this._visitor = visitor;
        this._bookName = bookName
        this._borrowDate = borrowDate
        this._returnDate = returnDate;

    }

}